# Directories will be set in a locals file, not the config

steps = ["source_find", "ap_phot"] # etc
targets = ["ngc1097"]
product = ["anchor"]
bands = ["F200W"]


[parameters.source_find]

# Background subtraction
sigma_to_clip = 3.0
coverage_mask = 
box_size = 
filter_size = 
bkg_estimator = 

# Source finding
do_source_find = true
finder = "iraf"
snr_threshold = 3.0
use_filter_fwhm = false
fwhm = 2.0
roundlo = -0.5
roundhi = 0.5
sharplo = 0.2
sharphi = 1.0
nsources = 50000


[parameters.ap_phot]

use_brightest = None
aperture_radius = 
phot_method = "exact"
radius_sky_in = 
radius_sky_out = 
sigma_to_cip = 3.0
maxiters = 5